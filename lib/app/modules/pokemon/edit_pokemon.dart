import 'package:flutter/material.dart';

class EditPokemon extends StatelessWidget {
  const EditPokemon({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}