import 'package:flutter/material.dart';

class NewPokemon extends StatelessWidget {
  const NewPokemon({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}